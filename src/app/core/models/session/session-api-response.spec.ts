import { SessionApiResponse } from './session-api-response';

describe('SessionApiResponse', () => {
  it('should create an instance', () => {
    expect(new SessionApiResponse()).toBeTruthy();
  });
});
